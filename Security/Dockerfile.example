FROM ubuntu:latest

RUN \
        apt-get update && \
        apt-get -y upgrade && \
        apt-get -y clean && \ 
	apt-get -y autoremove

RUN \
	adduser --system --no-create-home --group --disabled-password --shell /bin/false dockeru

CMD ["bash"]
